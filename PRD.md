# 12:55 — 제품 요구사항 정의서 (PRD)

**제품명** 12:55
**소속** KIPI Playground (한국특허정보원 e스포츠 게임 개발 공모전 / 팀 KIPlay)
**문서 버전** v1.0
**작성일** 2026-08-15
**상태** MVP 개발 착수

---

## 1. 한 줄 정의

매주 월요일 12시 55분, 점심시간 마지막 5분에 열리는 전사 실시간 OX 서바이벌 게임.

---

## 2. 배경과 문제 정의

### 2.1 공모전 맥락

한국특허정보원 e스포츠 게임 개발 공모전에 17개 팀이 참가 중이다. 다수의 팀이 기관 업무(특허 심사, 신입 육성, 업무 퀴즈)를 게임에 직접 이식하는 접근을 취하면서, 재미와 의미가 반비례하는 공통 문제를 겪고 있다.

우리 팀(KIPlay)은 **허브 로비 + 모듈형 미니게임** 구조의 플랫폼 전략을 택했다. 문서 심사 기준 3순위 평가를 받았으며, 평가에 기록된 강점과 약점은 다음과 같다.

- **강점** — 일회성 이벤트가 아닌 지속 확장 가능한 사내 e스포츠 문화의 기반이라는 포지셔닝, 부서 간 사일로 해소, 전광판 리더보드를 통한 관전성
- **약점** — 플랫폼형 다중 미니게임은 구현 난도가 높아, **실제로 돌아가는 데모가 없으면 오히려 감점 위험이 크다**

### 2.2 해결하려는 문제

세 가지 문제를 동시에 푼다.

1. **플랫폼이 살아 있음을 증명할 방법이 없다.** 심사위원이 17개 팀 데모를 다 본 뒤 남는 질문은 "행사 끝나고도 쓸까?"이며, 대부분의 팀은 여기에 답이 없다.
2. **우리 미니게임들도 결국 업무 퀴즈라 재미가 없다.** 재미를 담당하는 게임이 별도로 필요하다.
3. **사내 공지와 지식재산 동향을 아무도 읽지 않는다.** 조직 입장의 실질 효용이 필요하다.

### 2.3 접근

12:55는 **재미를 담당하는 게임이 아니라, 플랫폼이 매주 살아 움직인다는 증거**로 설계한다. 게임 자체의 재미는 지식이 아니라 **집단 동시 탈락의 드라마**에서 나온다. 문제 내용은 재미의 원천이 아니라 탈락을 만드는 장치일 뿐이다.

---

## 3. 목표와 비목표

### 3.1 목표 (Goals)

| # | 목표 | 성공 기준 |
|---|---|---|
| G1 | 심사 시점에 실운영 기록을 제시한다 | 누적 10회 이상 실제 개최, 실측 참여 데이터 확보 |
| G2 | 룰 설명 없이 즉시 참여 가능하다 | 최초 참여자가 안내 없이 1문항을 완주 |
| G3 | 전광판 관전이 성립한다 | 사무실 모니터에서 3m 거리 판독 가능 |
| G4 | 450명 동시 접속에서 안정적으로 동작한다 | 부하 리허설 2회 무사고 |
| G5 | 운영 부담이 주당 1분 이하다 | 출제 자동화 후 담당자 조작 시간 측정 |

### 3.2 비목표 (Non-goals)

- **학습 효과의 극대화** — 이 게임은 교육 콘텐츠가 아니다. 정답 공개 시 출처 링크를 노출하는 수준까지만 한다.
- **지식 실력의 공정한 측정** — 운의 개입을 의도적으로 크게 둔다. OX는 찍어서 맞을 수 있어야 한다.
- **긴 플레이 타임** — 5분 슬롯을 절대 넘기지 않는다. 재미를 위해 시간을 늘리지 않는다.
- **네이티브 앱** — 모바일 웹으로만 제공한다.
- **문제 자동 출제 파이프라인** — MVP 범위 밖. 별도 논의 후 착수한다 (11장 참조).

---

## 4. 대상 사용자

| 구분 | 규모 | 특성 | 설계 시사점 |
|---|---|---|---|
| 전 직원 | 약 450명 | 20~50대, 기술직·행정직 혼재 | 룰이 단순해야 한다 |
| 신입 (2년차 미만) | 약 60명 추정 | 사내 지식 열세 | 핸디캡 장치 필요 |
| 임원 (원장·실장) | 10명 내외 | 이벤트 참전 대상 | 특별 표시와 안전한 연출 |
| 관전자 | 참여자와 중복 | 탈락 후 남는 인원 | 탈락 후에도 볼 것이 있어야 한다 |
| 운영자 | 1~2명 | 우리 팀 | 조작이 3클릭 이내여야 한다 |

**접속 환경 가정** — 12시 55분 기준 참여자의 절반 이상이 구내식당 또는 외부 식당에서 **모바일**로 접속한다. 모바일 우선 설계는 선택이 아니라 전제다.

---

## 5. 게임 규칙 명세

### 5.1 진행 시나리오

| 시각 | 단계 | 소요 |
|---|---|---|
| 12:50:00 | 대기실 오픈 | 4분 30초 |
| 12:54:30 | 카운트다운, 입장 마감 | 30초 |
| 12:55:00 | 1~5문항 진행 | 약 50초 |
| 12:56:00 | 서든데스 (생존자 2명 이상 시) | 최대 30초 |
| 12:57:00 | 챔피언 발표 | 60초 |
| 12:58:00 | 종료 | — |

### 5.2 문항 규칙

- 문항 수는 **5문항** 고정.
- 각 문항은 **응답 7초 + 정답 공개 3초 = 10초**.
- 응답은 O 또는 X 중 하나. **미응답은 오답 처리**한다 (무응답으로 살아남는 것을 허용하지 않는다).
- 응답 제출 후 7초가 끝나기 전까지 **변경 가능**하다. 마지막 제출이 최종이다.
- 오답자는 정답 공개와 동시에 **즉시 탈락**한다.
- 응답 중 실시간으로 **O/X 선택 인원 분포**가 노출된다. 다수를 따를지 소신을 지킬지의 눈치싸움이 여기서 발생한다.

### 5.3 난이도 배분

전멸과 무탈락을 모두 방지하기 위해 5문항의 난이도를 고정 배분한다.

| 문항 | 난이도 | 목표 정답률 | 성격 |
|---|---|---|---|
| 1 | 쉬움 | 85% | 워밍업. 전원 생존에 가깝게 |
| 2 | 쉬움~중간 | 70% | |
| 3 | 중간 | 55% | |
| 4 | 어려움 | 40% | |
| 5 | 어려움 | 30% | 결승 |

문제 소재 구성은 **정통(특허법·지식재산 뉴스·사내 공지) 30% / 사내 가십·난센스 70%**를 유지한다. 지식 격차가 고착되면 신입과 비전문 부서가 조기 전멸해 참여 동기가 사라지기 때문이다.

### 5.4 부활권

- **입사 2년 미만** 직원에게 회차마다 **1개** 자동 지급.
- 자동 부활이 아니다. 탈락이 확정되는 순간 **3초 선택 창**이 뜨고 본인이 쓸지 말지 결정한다.
- 3초 내 미선택 시 **미사용으로 처리**하고 탈락을 확정한다.
- 부활 시 다음 문항부터 정상 참여한다.
- 부활권은 **서든데스에서는 사용할 수 없다**.
- 신입에게는 새싹 배지가 표시되어, 끝까지 생존 시 자연스럽게 응원 대상이 된다.

**설계 근거** — 지식 문제는 구조적으로 고참에게 유리하다. 부활권은 특혜가 아니라 균형 장치다.

### 5.4-A 층 상승과 종료 조건

정답을 맞힌 쪽의 바닥이 갈라져 한 층 위로 올라간다. 오답자는 그 층에 남는다. 탈락을 떨어뜨리는 대신 **남겨두는** 방식이라 톤이 덜 잔인하면서 정서적으로는 더 아프다.

**한 명만 남으면 문항이 남아 있어도 즉시 종료한다.** 그래야 회차마다 우승 층이 달라져 "이번 주는 4층 챔피언"이 이야깃거리가 된다. 5문항을 끝까지 가면 챔피언은 늘 최고층이라 층수에 변별력이 없다.

층 환경은 1층 로비, 그 위로 사무실 → 심사실 → 서고 → 전산실이 순환한다. **옥상은 게임 층이 아니라 우승 전용 장면**이며, 승자는 몇 층에서 이겼든 옥상에서 도시를 내려다본다.

정답 공개 시간은 층 상승 연출이 끝나기 전에 게임이 넘어가지 않도록 생존자 수에 따라 조절한다 — 100명 이상 4초, 100명 미만 5초, 10명 이하 6초. 이로 인해 한 판이 약 90초가 되며 3분 예산 안이다.

### 5.4-B 선택 공개 범위

생존자가 **100명 이상**이면 사람들이 좌우로 갈라지는 것이 실시간으로 보인다. 남의 판단을 참고할 수 있고, 이 관망은 오프라인 OX 퀴즈의 원래 재미이므로 막지 않는다.

생존자가 **100명 미만**이면 방향을 감춘다. 머리 위에 "선택함" 표시만 켜지고 어느 쪽인지는 보이지 않으며, 정답 공개 순간에야 전원이 갈라진다. 초반은 시끄러운 군중이고 후반은 조용하고 외로워지는 구조다.

> 이 가리기는 **반드시 서버에서** 수행한다. 클라이언트에서만 숨기면 개발자도구로 노출된다. 상품이 걸린 이상 타협할 수 없다.

### 5.5 서든데스

5문항 종료 후 생존자가 2명 이상이면 실행한다.

- **숫자 근접값 문제** 1개를 출제한다. 예: "지난주 우리 원 특허 출원 건수는?"
- 제한 시간 **15초**. 정답에 가장 가까운 값을 제출한 사람이 승리한다.
- 동일 오차가 발생하면 **제출 속도**로 가린다. 이때 판정 기준은 서버 수신 시각이 아니라 **클라이언트가 보고한 반응 시간**이다. 회선이 느린 참여자가 불이익을 받지 않게 하기 위함이다.
- 미제출자는 즉시 탈락 처리한다.
- 생존자가 0명이면 **직전 문항까지 생존했던 인원 전체**를 공동 준우승으로 처리하고 챔피언 없음으로 종료한다.

### 5.6 VIP 이벤트 — "원장님을 이겨라"

- 시즌당 2~3회 실행한다. 임원(원장·실장)이 동일한 사번으로 참전한다.
- VIP는 화면과 전광판에서 **왕관 표시**로 구분되고, 전광판에 "원장님 생존 중" 상태가 실시간으로 노출된다.
- **VIP보다 오래 생존한 참여자 전원**이 커피 상품권 5매 추첨 대상이 된다.
- VIP가 조기 탈락해도 문제가 되지 않도록 설계한다. 굴욕적 연출을 금지하고, 탈락 후에는 **명예 관전자**로 전환되어 한마디 코멘트를 남길 수 있게 한다.

### 5.7 시즌과 포인트

| 항목 | 규칙 |
|---|---|
| 참여 | 5점 |
| 생존 1문항당 | 10점 |
| 우승 | 50점 |
| VIP 격파 | 30점 |
| 시즌 | 12주 = 1시즌. 시즌 챔피언·부서 우승 시상 |
| 부서 랭킹 | 총점이 아닌 **참여율(%)** 기준. 소규모 부서 불이익 방지 |
| 스트릭 | **주 단위** 연속 참여. "8주 연속" 형태로 표시 |

> MVP에서는 포인트를 회차 내에서만 계산하고 영속 저장하지 않는다. 시즌 집계는 Post-MVP.

### 5.8 밸런스 장치

- 3회 연속 우승자는 다음 회차 **응답 시간이 1초 단축**된다 (7초 → 6초).
- 개인 순위만큼 **스트릭**과 **부서 참여율**을 화면 전면에 배치한다. 우승 확률이 낮은 다수를 붙잡는 것은 결국 이 두 지표다.

---

## 6. 화면 명세

### 6.1 화면 목록

| ID | 화면 | 대상 | MVP |
|---|---|---|---|
| S1 | 로그인 | 참여자 | ✅ |
| S2 | 대기실 | 참여자 | ✅ |
| S3 | 문항 진행 | 참여자 | ✅ |
| S4 | 정답 공개 | 참여자 | ✅ |
| S5 | 부활권 선택 | 참여자 (신입) | ✅ |
| S6 | 탈락 / 관전 | 참여자 | ✅ |
| S7 | 서든데스 | 참여자 | ✅ |
| S8 | 결과 / 챔피언 | 참여자 | ✅ |
| S9 | 전광판 | 사무실 모니터 | ✅ |
| S10 | 운영자 콘솔 | 운영자 | ✅ |

### 6.2 핵심 화면 요구사항

**S1 로그인** — 사번 입력 한 필드. 숫자 키패드가 바로 뜨도록 `inputmode="numeric"`. 사번에서 이름·부서·입사연차를 조회해 확인 후 입장.

**S2 대기실** — 12:55:00까지 남은 시간을 **초 단위 카운트다운**으로 크게 표시. 실시간 참가자 수가 올라가는 것이 보여야 한다. 부서별 참여 현황과 지난 회차 챔피언을 함께 노출한다. 이 화면은 재미 요소인 동시에 **접속 스파이크를 5분에 걸쳐 분산시키는 부하 설계**다.

**S3 문항 진행** — 문항 텍스트는 모바일에서도 한눈에 들어와야 한다. 화면 하단 절반을 **O / X 두 개의 거대 버튼**이 양분한다. 상단에 7초 타이머가 시각적으로 줄어들고, O와 X를 각각 몇 명이 골랐는지 실시간으로 갱신된다. 엄지 한 손 조작으로 완결되어야 한다.

**S4 정답 공개** — 정답 측이 강조되고 오답 측이 즉시 죽는다. 생존자 수가 감소하는 것이 숫자로 보여야 한다. 정답 근거가 된 원문 링크를 함께 노출한다.

**S5 부활권** — 3초 카운트다운과 함께 사용/포기 선택. 긴장감이 이 화면의 목적이다.

**S6 탈락 / 관전** — 탈락은 명확하고 극적으로 알린다. 다만 탈락 후에도 남은 문항과 생존자 수를 계속 볼 수 있어야 한다. 여기서 이탈하면 관전자가 사라진다.

**S9 전광판** — **3m 거리 판독**이 절대 기준이다. 생존자 카운트가 가장 크고, O/X 분포 바와 탈락 피드가 뒤따른다. 개인 조작 요소는 없다. VIP 생존 여부를 별도 배지로 표시한다.

**S10 운영자 콘솔** — 게임 시작, 다음 단계 강제 진행, 리셋. 조작 3클릭 이내. 시연 중 사고 시 복구 수단이다.

### 6.3 디자인 원칙

- **다크 아레나** — 심연에 가까운 인디고 블랙 바탕에 앰버 신호색. 시계·전광판·출발안내판의 세계관.
- **모든 숫자는 고정폭(tabular)** — 카운트다운과 인원수가 흔들리지 않아야 한다.
- **모션은 상태 변화에만 쓴다** — 장식적 애니메이션을 금지한다. 탈락·정답 공개·카운트다운처럼 정보가 바뀌는 순간에만 모션을 배정한다.
- **접근성** — `prefers-reduced-motion` 존중, 포커스 링 유지, 색상만으로 정답/오답을 구분하지 않는다(형태와 텍스트 병기).

---

## 7. 기술 아키텍처

### 7.1 설계 판단

이 게임은 실시간 액션이 아니라 **방송형 동기 게임**이다. 전원이 같은 문제를 동시에 받고, 7초 안에 한 글자를 보내고, 결과를 함께 받는다. 이 성질에서 세 가지 결정이 따라온다.

**결정 1 — WebSocket이 아니라 SSE + POST.**
양방향 실시간 채널이 필요 없다. 서버→클라이언트 방송은 SSE(Server-Sent Events) 단방향, 클라이언트→서버 답안은 평범한 HTTP POST로 처리한다. 구현이 단순해지고 시연 당일 실패 확률이 낮아진다.

**결정 2 — 서버리스가 아니라 상시 단일 프로세스.**
서버리스 환경은 장시간 연결 유지에 부적합하고 스파이크마다 콜드스타트가 발생한다. Node 프로세스 하나를 상시 띄우는 편이 450명 규모에서 더 싸고 안정적이다.

**결정 3 — 스케일아웃하지 않는다.**
인스턴스를 늘리는 순간 세션 고정과 Redis 공유 상태가 필요해져 복잡도만 오른다. 450명은 단일 인스턴스가 정답이다.

### 7.2 스택

| 계층 | 선택 | 근거 |
|---|---|---|
| 서버 | **Node.js 내장 `http` 모듈만** | 의존성 0. `npm install` 실패 리스크 제거 |
| 실시간 | **SSE** | 단방향 방송에 최적, 자동 재연결 내장 |
| 클라이언트 | **바닐라 HTML/CSS/JS** | 빌드 스텝 없음. 파일 열면 즉시 실행 |
| 상태 | **인메모리** | 회차 중 상태는 전부 메모리, DB 쓰기는 문항 종료 시 배치 |
| 문제 | **JSON 파일** | 파이프라인 연동 전까지의 공급 방식 |

> **런타임 외부 요청 금지.** CDN 스크립트·웹폰트·원격 이미지를 일절 사용하지 않는다. 사내망 환경과 오프라인 시연 모두에서 동작해야 하기 때문이다.

### 7.3 상태 기계

```
idle ──start──> lobby ──▶ question(n) ──▶ reveal(n) ──▶ [revive] ──┐
                                    ▲                              │
                                    └───────── n < 5 ──────────────┘
                                                    │ n = 5
                                                    ▼
                                        [sudden] ──▶ result ──reset──> idle
```

| 상태 | 지속 | 전이 조건 |
|---|---|---|
| `idle` | — | 운영자 시작 |
| `lobby` | 설정값 (기본 30초, 실전 5분) | 타이머 만료 또는 운영자 강제 |
| `question` | 7초 | 타이머 만료 |
| `reveal` | 3초 | 타이머 만료 |
| `revive` | 3초 | 대상자 존재 시에만 진입 |
| `sudden` | 15초 | 생존자 2명 이상일 때만 진입 |
| `result` | — | 운영자 리셋 |

### 7.4 API 명세

**참여자**

| 메서드 | 경로 | 요청 | 응답 |
|---|---|---|---|
| POST | `/api/login` | `{empId}` | `{token, user}` |
| GET | `/api/stream?token=` | — | SSE 스트림 |
| POST | `/api/answer` | `{token, qIndex, answer, rt}` | `{ok}` |
| POST | `/api/revive` | `{token, use}` | `{ok}` |
| POST | `/api/sudden` | `{token, value, rt}` | `{ok}` |

**전광판 / 운영**

| 메서드 | 경로 | 요청 | 응답 |
|---|---|---|---|
| GET | `/api/spectate` | — | SSE 스트림 (인증 불필요) |
| POST | `/api/admin/start` | `{key, lobbySec}` | `{ok}` |
| POST | `/api/admin/next` | `{key}` | `{ok}` |
| POST | `/api/admin/reset` | `{key}` | `{ok}` |

`rt`는 클라이언트가 측정한 반응 시간(ms)이다. 서든데스 동률 판정에 사용한다.

**SSE 이벤트**

| 이벤트 | 페이로드 | 용도 |
|---|---|---|
| `snapshot` | 전체 상태 | 접속·재접속 시 1회 |
| `lobby` | `{joined, remainMs}` | 참가자 수 갱신 |
| `question` | `{index, text, endsAt, difficulty}` | 문항 공개 |
| `tally` | `{o, x, alive}` | 실시간 분포 (1초 간격) |
| `reveal` | `{answer, source, eliminated, alive}` | 정답 공개 |
| `revive` | `{deadline}` | 대상자에게만 전송 |
| `sudden` | `{text, endsAt}` | 서든데스 시작 |
| `result` | `{champion, ranking, vipBeaten}` | 종료 |

### 7.5 부하 설계

450명 전원 접속을 가정한 추정치다. 주 1회 편성은 참여를 한 시점에 집중시키므로 모수 전체가 들어온다고 본다.

| 항목 | 1회 게임 기준 추정 | 판정 |
|---|---|---|
| 동시 접속 | 450 SSE 연결 | 여유 |
| 답안 제출 | 문항당 450건 / 7초 → 평균 64 req/s, 최대 약 150 req/s | 여유 |
| 전송량 | 브로드캐스트 135 KB/회, 게임 전체 약 2 MB | 여유 |
| DB 쓰기 | 2,250 rows / 배치 5회 | 여유 |
| 서버 메모리 | 200 MB 미만 | 여유 |
| **접속 스파이크** | 12:54:59 집중 시 450 handshake/s | **설계 필요** |

**스파이크 대응**
- 12:50 대기실 선오픈으로 접속을 5분간 분산한다.
- 대기실에서 정적 자산과 게임 화면을 미리 내려받게 한다.
- 연결 끊김 시 클라이언트는 **0~3초 무작위 지연 후** 재접속한다 (thundering herd 방지).
- `tally` 브로드캐스트는 매 응답이 아니라 **1초 간격 집계**로 보낸다.
- 시스템 파일 디스크립터 한도(ulimit)를 4096 이상으로 설정한다.

**공정성** — 응답 7초는 넉넉하므로 100ms 수준의 네트워크 지연은 결과에 영향을 주지 않는다. 서든데스 속도 판정만 클라이언트 보고 반응 시간을 사용한다.

**검증** — k6 또는 Artillery로 가상 사용자 450명 부하 리허설을 사전 2회 실시한다. 오프닝 시즌 2주(10회)가 그 자체로 실전 부하 데이터 수집 기간이다.

### 7.6 인증과 부정 방지

| 항목 | MVP | 정식 |
|---|---|---|
| 인증 | 사번 + 인메모리 명부 조회 | 사내 SSO 연동 |
| 세션 | 발급 토큰, 동시 세션 1개 제한 | 동일 + 기기 토큰 |
| 부정 시도 | 중복 로그인 차단 | 이상 패턴 로깅, 추첨은 사번 단위 집계 |

사번은 식별자일 뿐 인증 수단이 아니다. 상품이 걸린 이상 정식 운영에는 SSO가 필요하다. 연동이 어려우면 사내 메일 1회 인증 후 기기 토큰을 발급한다.

**사번 체계** — 사내 사번은 **5자리**다. 명부에 등록된 사번은 `joinYear`를 그대로 쓰고, 미등록 사번은 `data/employees.json`의 `idFormat` 규칙으로 입사연도를 추정한다. 기본값은 앞 2자리를 두 자리 입사연도로 해석하는 방식이며(`26005` → 2026년 입사), 이 덕분에 450명 명부를 채우지 않고도 부하 테스트와 체험을 돌릴 수 있다. 사내 체계가 순번제라면 `yearPrefix`를 `0`으로 두고 `roster`를 인사 데이터로 채우면 되며 코드 수정은 필요 없다. 연차는 부활권 지급 여부를 가르는 유일한 입력이므로 정식 운영 전에 인사 데이터와 대조가 필요하다.

---

## 8. MVP 범위

### 8.1 포함

- 사번 로그인, 인메모리 직원 명부
- 대기실 (카운트다운, 실시간 참가자 카운터)
- 5문항 OX 진행 (7초 + 3초), 실시간 O/X 분포
- 오답 즉시 탈락
- 신입 부활권 (3초 선택)
- 서든데스 (숫자 근접값, 반응 시간 동률 판정)
- 챔피언 결과 화면
- VIP 모드 (왕관, "원장님 생존 중", VIP 격파 집계)
- 전광판 관전 화면
- 운영자 콘솔 (시작 / 강제 진행 / 리셋)
- 문제 JSON 파일 공급
- SSE 자동 재접속 + 지터
- **체험 모드** — 인원과 시간 제약 없이 혼자 전체 흐름을 확인. 빈자리를 봇이 채우며, 봇은 난이도별 목표 정답률(5.3)에 맞춰 응답한다. 역할(신입·일반·VIP)을 골라 부활권과 VIP 이벤트까지 확인할 수 있다
- **군중 무대** — Canvas 단일 레이어. 인원에 따른 6단계 LOD, 좌우 이동, 층 상승, 층별 환경, 미니 타워 표시. 전광판이 주 무대이고 참여자 폰은 축소 모드로 상단에 배치한다
- **본부 6색** — 상위 조직 5개 + 기타. 부서명은 근접 LOD에서 텍스트로 노출
- **소리** — 징·나레이션 포함 전부 런타임 합성. 음원 파일과 네트워크 요청 없음. 전광판은 오디오 잠금 해제 버튼 필요

### 8.2 제외 (Post-MVP)

| 항목 | 사유 |
|---|---|
| **AI 자동 출제 파이프라인** | 별도 논의 후 착수 |
| 사내 SSO 연동 | 인프라 협의 필요 |
| 시즌 포인트 영속화 / DB | MVP는 회차 내 계산만 |
| 부서 랭킹 대시보드 | 데이터 축적 이후 |
| 지난 회차 아카이브 | 공백 6일 리텐션 장치, 2차 |
| 푸시 알림 | 사내 메신저 연동 협의 필요 |

---

## 9. 성공 지표

| 지표 | 목표 | 측정 |
|---|---|---|
| 회차 참여율 | 전 직원의 40% 이상 | 참여 사번 수 / 450 |
| 재참여율 | 2회 이상 참여자 60% 이상 | 누적 참여자 기준 |
| 완주율 | 탈락 후 결과 화면까지 잔류 70% 이상 | 관전 SSE 연결 유지 |
| 평균 소요 | 3분 이내 | 게임 시작~결과 |
| 장애 | 회차당 0건 | 서버 에러 로그 |
| 운영 조작 시간 | 주당 1분 이하 | 담당자 실측 |

---

## 10. 일정

| 주차 | 산출물 |
|---|---|
| W1 | **MVP 게임 엔진** — 본 문서의 8.1 전체. 문제는 수동 공급 |
| W2–3 | **오프닝 시즌** — 협조 부서 대상 매일 운영 10회. 룰 튜닝과 부하 데이터 확보 |
| W4 | **출제 자동화** — 파이프라인 착수 (별도 논의 후) |
| W5 | **정규 시즌 개막** — 전사 오픈, 부하 리허설 완료, 첫 VIP 이벤트 |

---

## 11. 미결 사항

### 11.1 문제 공급 파이프라인 (별도 논의)

MVP는 **JSON 파일에서 문제를 읽는 구조**로 만든다. 파이프라인은 이 인터페이스만 맞추면 되도록 경계를 명확히 둔다.

```json
{
  "id": "q-2026-08-17-01",
  "text": "특허 출원 후 심사청구는 5년 이내에 해야 한다.",
  "answer": "O",
  "difficulty": "easy",
  "category": "patent-law",
  "source": { "title": "특허법 제59조", "url": "https://..." },
  "evidence": "심사청구는 출원일부터 3년 이내에 하여야 한다."
}
```

논의가 필요한 항목:
- 수집 소스 범위 (특허청 보도자료 / 지식재산 전문지 / 사내 게시판)
- 사내 게시판 크롤링 권한 및 개인정보 필터링 정책 — **보안 부서 사전 협의 필요**
- 할루시네이션 차단 규칙 (근거 원문 문장이 없는 문항 자동 폐기)
- 과거 정답률 기반 난이도 자동 라벨링
- 담당자 검수 UI

### 11.2 확인 필요 사항

| 항목 | 심각도 | 내용 |
|---|---|---|
| **외부망 접속 가능 여부** | 높음 | 사내망 전용이면 12:55 시간대의 전제가 무너진다. 외부 식당에서 접속이 안 되면 시각을 11:30으로 되돌려야 한다 |
| 사내 게시판 크롤링 권한 | 높음 | 보안 부서 협의. 불가 시 공개 지식재산 뉴스만으로 동작하도록 대비 |
| 근무시간 게임 승인 | 중간 | 점심시간 마지막 5분이라 명분은 있으나 전사 도입 시 공식 승인 필요 |
| 상품 예산 | 중간 | 오프닝 시즌 집중 배정, 정규 시즌은 소액 + 시즌 시상 |
| 사내 모니터 사용 | 낮음 | 전광판 송출 위치와 권한 |

---

## 12. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 데모 당일 서버 장애 | 치명적 | 의존성 0 구성, 운영자 콘솔로 강제 진행·리셋, 오프닝 시즌 10회 사전 검증 |
| 주 1회라 잊힌다 | 높음 | 금요일 예고, 월요일 아침 알림, 주 단위 스트릭, 아카이브 |
| 지식 격차로 신입 조기 전멸 | 높음 | 난센스 70% 배분, 신입 부활권 |
| 같은 사람만 우승 | 중간 | 3연승 핸디캡, 스트릭·참여율 지표 전면 배치 |
| 상품 노린 다중 계정 | 중간 | 동시 세션 1개 제한, 사번 단위 집계 |
| VIP 조기 탈락으로 인한 민망함 | 낮음 | 굴욕 연출 금지, 명예 관전자 전환, 사전 브리핑 |

---

## 부록 A. 용어

| 용어 | 정의 |
|---|---|
| 회차 | 1회의 게임 (약 3분) |
| 시즌 | 12주 단위 묶음 |
| 오프닝 시즌 | 출시 직후 2주간 매일 운영하는 창단 이벤트 |
| 전광판 | 사무실 모니터에 송출하는 관전 전용 화면 |
| VIP | 이벤트 참전 임원 |
| 부활권 | 2년차 미만에게 지급되는 회차당 1회 부활 기회 |
| 서든데스 | 5문항 후 생존자가 복수일 때의 숫자 근접값 단판 |
