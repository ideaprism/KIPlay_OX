'use strict';

/**
 * 서버리스 진입점.
 *
 * 주의 — 이 게임은 인메모리 상태를 가진 단일 상시 프로세스를 전제로 설계했다(PRD 7.1).
 * 서버리스 환경에서는 요청이 서로 다른 인스턴스로 흩어질 수 있고, 그러면 인스턴스마다
 * 게임 상태가 따로 놀아 로그인 토큰이 무효로 보이거나 회차가 보이지 않는다.
 * 소수 인원의 체험용으로만 쓰고, 실제 회차는 상시 프로세스에 올려야 한다.
 */

module.exports = require('../server.js');
